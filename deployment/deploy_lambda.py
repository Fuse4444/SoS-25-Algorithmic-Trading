# For AWS Lambda Zip Packaging
# 1. pip install packages to ./package
# 2. zip -r9 function.zip . (after copying script into package dir)

print("Use AWS Console or CLI to upload zipped handler script and dependencies.")